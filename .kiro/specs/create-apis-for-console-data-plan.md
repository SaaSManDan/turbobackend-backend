- Create API to send request from MCP and enqueue on background job processor

- Create API to get files from S3
- Create API to get MCP key
- Create API to get database tables and structures
- Create API to get database records (might combine with above API)
- Create API to show backend applications being hosted and the link to them
- Create API to show API endpoints
- Create API to show all activity logs

- Create API to connect to github, get back access key and store it in AWS Parameter Store SecureString
- Create API to connect to digital ocean, get back access key and store it in AWS Parameter Store SecureString
- Create API to connect to aws, get back access key and store it in AWS Parameter Store SecureString (or use oauth)